import { CurrentlyCreatedReadingList } from "../types";

const defaultList: CurrentlyCreatedReadingList = {
  name: "",
  description: "",
  shared: false,
  issues: [],
  userId: "",
  coverImg: undefined,
};

export default defaultList;
